# Excel图片提取工具依赖包
# 基础依赖（Python标准库，无需安装）
# - tkinter (GUI界面)
# - zipfile (ZIP文件处理)
# - xml.etree.ElementTree (XML解析)
# - os, shutil, re, pathlib (文件操作)
# - datetime, threading (时间和线程)

# 可选增强依赖（如需要更强大的Excel处理功能）
# openpyxl>=3.0.0  # Excel文件处理（可选，用于更复杂的Excel操作）

# 图片合并功能依赖
pillow>=8.0.0    # 图片处理（用于图片合并功能）
pyinstaller>=4.0  # 打包工具（可选，用于将脚本打包成独立的可执行文件）
pywin32-ctypes>=0.2.0  # 用于解决打包时的Win32 API问题

# 注意：本工具主要使用Python标准库，图片合并功能需要安装Pillow库
# 安装命令：pip install pillow